# Secrets — never commit. Deploy secrets live on the server / in CircleCI contexts.
.env
.env.*
*.pem
*.key
secrets/
*-token
*.secret
*.env
*-key
id_rsa*
id_ed25519*
*.p12
*.pfx

# Local design export (source material; migrated into site/ by scripts/).
design-src/

# GitHub profile repo (RealRogerWinter/RealRogerWinter) — its own git repo that
# happens to sit in this working tree; not part of the portfolio site.
/RealRogerWinter/

# Tooling / OS cruft
node_modules/
__pycache__/
test-results/
.DS_Store
*.log
*.tmp
.cache/

# Playwright run output (reports + per-run actual/diff images). The committed
# baselines under tests/visual/__screenshots__/ are NOT ignored — they are the
# look-oracle and must be tracked.
playwright-report/
blob-report/
/test-results/

# Claude Code session / worktree scratch dir — never project content
/.claude/
